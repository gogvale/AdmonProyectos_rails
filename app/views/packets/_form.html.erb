<%= form_with(model: packet) do |form| %>
  <% if packet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(packet.errors.count, "error") %> prohibited this packet from being saved:</h2>

      <ul>
        <% packet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.select :status, Packet.statuses.keys %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
